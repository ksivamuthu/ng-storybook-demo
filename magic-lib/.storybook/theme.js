import { create } from '@storybook/theming/create';
import logo from './logo.svg';

export default create({
  base: 'light',
  brandTitle: 'Magic Storybook',
  brandUrl: 'https://magic.com',
  brandImage: logo,
});
